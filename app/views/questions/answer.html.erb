<h1><%=t 'question.answer' %></h1>

<%= render :partial => "correct" if @right_answer %>
<%= render :partial => "incorrect" if !@right_answer %>

<%= link_to t('question.again'), ask_path(:id => @question.id), class: "btn btn-large btn-primary" %>
<%= link_to t('question.make'), make_path %>
