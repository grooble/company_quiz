<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: t('post.compose') %>
  </div>
  <%= f.submit t('post.submit'), class: "btn btn-large btn-primary" %>
<% end %>